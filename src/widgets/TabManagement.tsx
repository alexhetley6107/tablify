import { Stack } from '@mui/material';
import { FC } from 'react';
import { ContentView } from '../shared/ui';

export const TabManagement: FC = () => {
  return (
    <Stack height="100%">
      <ContentView title={'Title'} />
    </Stack>
  );
};
